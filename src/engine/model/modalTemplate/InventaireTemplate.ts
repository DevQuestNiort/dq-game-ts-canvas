import {AbstractModalTemplate, TypeModal} from "./AbstractModalTemplate.ts";

export class InventaireTemplate extends AbstractModalTemplate{


    constructor() {
        super(TypeModal.INVENTAIRE);
    }
}